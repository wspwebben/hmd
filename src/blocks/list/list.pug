mixin list(items)
  .list&attributes(attributes)
    h2.visually-hidden list
    ul.list__list
      each item in items
        li.list__item
          h3.list__title= item.title
          p.list__text= item.text
